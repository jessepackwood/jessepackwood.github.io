import React from 'react';
import PropTypes from 'prop-types';

const Instructions = props => {
  return (
    <div>
      <h1>Adobe Lightroom Installation Instructions</h1>
    </div>
  );
};

Instructions.propTypes = {
  
};

export default Instructions;